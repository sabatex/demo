<Window x:Class="Lesson19_Animation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lesson19_Animation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Width="50" Height="20" Grid.Column="1" Grid.Row="1" Background="Gray">
            <Button.Triggers>
                <EventTrigger RoutedEvent="Button.MouseEnter">
                    <EventTrigger.Actions>
                        <BeginStoryboard HandoffBehavior="Compose">
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width"
                                                 From="{Binding RelativeSource={RelativeSource self},Path=ActualWidth}"
                                                 To="300" Duration="0:0:0.5"/>
                                <DoubleAnimation Storyboard.TargetProperty="Height"
                                                 From="{Binding RelativeSource={RelativeSource self},Path=ActualHeight}"
                                                 To="140" Duration="0:0:0.5"/>
                                <ColorAnimation To="Green" 
                                    Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)" 
                                    Duration="0:0:0.5"/>
 
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger.Actions>
                </EventTrigger>
                <EventTrigger RoutedEvent="Button.MouseLeave">
                    <EventTrigger.Actions>
                        <BeginStoryboard HandoffBehavior="Compose">
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width"
                                                 From="{Binding RelativeSource={RelativeSource self},Path=ActualWidth}"
                                                 To="50" Duration="0:0:0.5" DecelerationRatio="0.5"/>
                                <DoubleAnimation Storyboard.TargetProperty="Height"
                                                 From="{Binding RelativeSource={RelativeSource self},Path=ActualHeight}"
                                                 To="20" Duration="0:0:0.5"/>



                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger.Actions>
                </EventTrigger>
            </Button.Triggers>
            <Button.Content>
                Hello
            </Button.Content>
        </Button>

    </Grid>
</Window>
