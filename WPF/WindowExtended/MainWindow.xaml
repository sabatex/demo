<Window x:Class="WindowExtended.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowExtended"
        mc:Ignorable="d"
        Height="450" Width="800" WindowStyle="None"  
        x:Name="mainWindow" WindowChrome.IsHitTestVisibleInChrome="True"
        Icon="C:\Users\serhiy\source\repos\WindowsFormsApp1\WindowExtended\ntics software.jpg">
    <Window.CommandBindings>
        <CommandBinding Command="local:MainWindow.GenerateTree" Executed="CommandBinding_Executed" CanExecute="CommandBinding_CanExecute_1"/>
        <CommandBinding Command="local:MainWindow.ClearTree" Executed="CommandBinding_Executed_1" CanExecute="CommandBinding_CanExecute"/>
    </Window.CommandBindings>
    <DockPanel>
        <Grid DockPanel.Dock="Top" Background="{Binding ElementName=mainWindow, Path=Background}"  Height="Auto"  MouseLeftButtonDown="Grid_MouseLeftButtonDown" WindowChrome.IsHitTestVisibleInChrome="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
           </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" x:Name="p1">
                <Image Margin="0,5,0,0" Source="{Binding ElementName=mainWindow,Path=Icon}" Height="16" VerticalAlignment="top"/>
                <Menu Background="{Binding ElementName=mainWindow, Path=Background}" FontSize="14">
                    <MenuItem Header="File">
                        <MenuItem Command="local:MainWindow.GenerateTree"/>
                        <MenuItem Command="local:MainWindow.ClearTree"/>
                    </MenuItem>
                    <MenuItem Header="Edit"></MenuItem>
                    <MenuItem Header="View"></MenuItem>
                </Menu>
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top" WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Click="Minimize_Click" Content="&#xE949;" FontFamily="Segoe MDL2 Assets" FontSize="10" Padding="15,15,15,5" Background="Transparent" BorderBrush="Transparent" />
                <Button Click="Maximize_Click" FontFamily="Segoe MDL2 Assets" FontSize="10" Padding="15,10" Background="Transparent" BorderBrush="Transparent">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Button.Content" Value="&#xE739;" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Path=WindowState}" Value="Maximized">
                                    <Setter Property="Button.Content" Value="&#xE923;" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Click="Close_Click" Content="&#xE106;" FontFamily="Segoe MDL2 Assets" FontSize="10" Padding="15,10" Background="Transparent" BorderBrush="Transparent" />
            </StackPanel>
        </Grid>
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>

                <Button>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <Canvas>
                                                    <Polygon Fill="Beige" Points="100,0 0,200 200,200  " />
                                                    <Polygon Fill="Beige" Points="0,70 200,75 100,275  " />
                                                </Canvas>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>

                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Canvas >
                        <Polygon Fill="Beige" Points="100,0 0,200 200,200  " />

                    </Canvas>
                </Button>

            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Label>LogicalTree</Label>
                    <Label Grid.Row="1">VisualTree</Label>
                    <Label Grid.Column="1" x:Name="logicalTree"/>
                    <Label Grid.Row="1" Grid.Column="1" x:Name="visualTree"/>
                </Grid>
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <Button Margin="4" BorderBrush="YellowGreen" BorderThickness="2" Command="local:MainWindow.GenerateTree">Run</Button>
                    <Button Margin="4" BorderBrush="YellowGreen" BorderThickness="2" Command="local:MainWindow.ClearTree">Clear</Button>
                </StackPanel>
            </Grid>

        </Grid>
    </DockPanel>
</Window>
